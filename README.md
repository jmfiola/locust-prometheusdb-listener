# locust-prometheusdb-listener

# Installation 
```bash
pip install locust-prometheusdb-listener
```

# local installation
with pip
```bash
pip install -e ".[dev]"
```
or with poetry (a bit more modern):
```bash
poetry install
```